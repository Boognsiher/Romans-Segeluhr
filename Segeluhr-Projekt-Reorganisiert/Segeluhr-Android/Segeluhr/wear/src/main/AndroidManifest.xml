<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- BLE Central-Rolle (Uhr verbindet sich zum Handy-GATT-Server), analog
         zur bisherigen T-Watch-Ultra-Firmware (die einzige Boots-Uhr mit
         BLE-Central-Rolle zum Handy — die S3 ist reine Land-Uhr), siehe
         BLE_Protokoll.md und docs/Erweiterung_GalaxyWatch_WearOS.md. Ab API 31 ersetzen
         BLUETOOTH_SCAN/BLUETOOTH_CONNECT die alten BLUETOOTH*-Permissions;
         neverForLocation, weil nur nach der bekannten Service-UUID gefiltert
         wird, keine Positionsbestimmung über den Scan stattfindet. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Läuft NICHT eigenständig ohne das gekoppelte Handy - bezieht alle
             Nav-/Wind-/Renndaten per eigener BLE-Verbindung von der
             Segeluhr-Handy-App, siehe docs/Erweiterung_GalaxyWatch_WearOS.md. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
