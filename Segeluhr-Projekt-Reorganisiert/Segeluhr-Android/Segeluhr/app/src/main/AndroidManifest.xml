<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Automatische See-Erkennung (Overpass-API), siehe
         docs/Erweiterung_Automatische_See_Erkennung.md - nur beim einmaligen
         Einrichten gebraucht, danach läuft alles offline. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- BLE Peripheral-Rolle (Handy = GATT-Server für die T-Watch), siehe BLE_Protokoll.md -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Zusätzlich BLE Central-Rolle im Land-Modus (Handy = Scanner/Client zur
         Land-Uhr), siehe docs/Erweiterung_Landuhr_Kartenansicht.md -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Vibration für Signalmuster -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Dauerhafter Betrieb (GPS + BLE-Advertising) im Hintergrund -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Segeluhr"
        android:theme="@style/Theme.Segeluhr"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:keepScreenOn="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Segeluhr">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ble.SegeluhrForegroundService"
            android:exported="false"
            android:foregroundServiceType="location|connectedDevice" />

        <!-- Für DiagnosticsLogger.shareUri() (Setup-Tab "Diagnose-Log teilen"),
             siehe docs/Erweiterung_Diagnose_Log.md -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
